# Sessão 2026-08-11 — Unificação de projetos CloserPace (handoff Claude → Cursor)

> Registo de sessão no formato Decisões / Ações / Bloqueios.
> Contexto: thread Slack de unificação dos projetos (`jcol`, `pacelkin`, `manual-pace-cursor`) e centralização de conhecimento no Obsidian/JC-Brain.
> Papel do Claude nesta sessão: **plano e mapa**. A execução técnica no repo fica do lado do Cursor.

## Decisões (respostas do JC)

1. **Código da sessão** — não existe do lado do JC. A necessidade de unificação surgiu agora por o trabalho do livro não constar no vault principal.
2. **Estratégia — (C) monorepo / fundir fisicamente.** Assumida como o caminho mais arriscado (não era o caminho documentado até agora). Objetivo: o Obsidian ter a informação centralizada, maior coerência e evitar trabalho duplicado.
3. **Nomenclatura** — "Closepace" foi lapso; o correto é **"Closerpace"** (closerpace.com). **Não** se trabalha este ponto nesta sessão.
4. **`pacelkin` não é legado** — é projeto/funcionalidade em **desenvolvimento final**, integrado no closerpace.com.

## Mapa — decisões anteriores encontradas neste repo (`pacelkin`)

- **closerpace.com = base central / hub; `pacelkin` = módulo/ferramenta** que se liga a ela.
  - Fonte: `imported/docs/INSTRUCAO_WORKSPACE_CLOSERSPACE.md`
- **Integração já implementada = SSO (cenário C).** JWT HS256, claims `user_id/email/name/exp/iat`, segredo partilhado `CLOSERSPACE_PACELKIN_SSO_SECRET` (Supabase). Rota `/sso` valida o token em `imported/app/main.py`.
  - Fonte: `imported/docs/INTEGRACAO_CLOSERSPACE.md` (cenários A–E)
- **Merge de repos: decisão em aberto.** `INSTRUCAO_WORKSPACE_CLOSERSPACE.md` deixa explícito escolher entre `pacelkin` como **repo separado (git submodule)** ou **todo o código dentro do repo closerpace.com** (Opção A = multi-root workspace vs Opção B = monorepo `apps/pacelkin/`).
- **`jcol`** = site/app CloserPace, atualmente em `jcol.lovable.app` (backend Supabase), pré-lançamento de closerpace.com.
- Não existe neste repo: `memory/` prévio, doc de "fontes de verdade", nem referências a `jcol_local` / JC-Brain / Obsidian / vault.

## Bloqueios / questões para o @Cursor (lado `jcol_local`)

1. **(C) vs documentação existente.** O repo `pacelkin` já contempla a Opção B (monorepo `apps/pacelkin/`) como opção **em aberto**; do lado `jcol`, a documentação (segundo o Cursor) diz "repos separados, `pacelkin` já integrado ao nível de produto". Antes de executar (C) é preciso resolver este conflito.
2. **Forma do (C).** Se avançar: `pacelkin` como **subpasta** (`apps/pacelkin/`) do repo closerpace.com, ou como **git submodule**? (decisão deixada em aberto em `INSTRUCAO_WORKSPACE_CLOSERSPACE.md`).
3. **Incongruência interna do `pacelkin`.** Os docs na raiz descrevem um CLI aspiracional (`src/`, que não existe); a app real vive em `imported/`. Consolidar raiz vs `imported/` antes de fundir, para não arrastar duplicação para o monorepo.
4. **Nomenclatura (só registo).** Três grafias em uso: `Closepace` (lapso), `Closerpace` (correto/domínio), `Closerspace` (nomes de ficheiros e env vars neste repo, ex. `INSTRUCAO_WORKSPACE_CLOSERSPACE.md`, `CLOSERSPACE_PACELKIN_SSO_SECRET`). Por decisão do JC, **não** se altera nesta sessão.

## Próximo passo

Handoff ao @Cursor: inventário e execução no lado `jcol_local` + decisão **submódulo vs subpasta** para materializar a estratégia (C). Nenhum ficheiro deste repo foi movido/fundido/apagado nesta sessão.
